CREATE DATABASE:-

CREATE DATABASE sales_analysis;
USE sales_analysis;

CREATE TABLE:-

CREATE TABLE superstore (
    `Row ID` INT,
    `Order ID` TEXT,
    `Order Date` TEXT,
    `Ship Date` TEXT,
    `Ship Mode` TEXT,
    `Customer ID` TEXT,
    `Customer Name` TEXT,
    `Segment` TEXT,
    `Country` TEXT,
    `City` TEXT,
    `State` TEXT,
    `Postal Code` INT,
    `Region` TEXT,
    `Product ID` TEXT,
    `Category` TEXT,
    `Sub-Category` TEXT,
    `Product Name` TEXT,
    `Sales` DOUBLE
);